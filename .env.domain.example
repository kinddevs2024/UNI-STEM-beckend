# Backend domain CORS/URL env
# Copy needed values into your real .env file and replace placeholders.

# Main frontend URL (Olympiad; used by CORS and links)
FRONTEND_URL=https://global-olimpiads.biz

# Allowed origins for Olympiad API (pages/api/*) – comma-separated, no spaces
# Add both Olympiad and Portfolio fronts so both can call the same backend.
ALLOWED_ORIGINS=https://global-olimpiads.biz,http://173.249.47.147:3005

# Allowed origins for Portfolio API (/api/portfolio/*) – comma-separated
# Same list so Olympiad and Portfolio fronts work without CORS errors.
PORTFOLIO_CORS_ORIGIN=https://global-olimpiads.biz,http://173.249.47.147:3005

# Optional server binding
HOST=0.0.0.0
PORT=3000
