# Backend domain CORS/URL env
# Copy needed values into your real .env file and replace placeholders.

# Main frontend URL (exact origin)
FRONTEND_URL=https://global-olimpiads.biz

# Allowed origins for CORS (comma-separated, no spaces)
# Include both www and non-www if you use both.
ALLOWED_ORIGINS=https://global-olimpiads.biz,https://www.global-olimpiads.biz

# If you use a separate API subdomain, include it too:
# ALLOWED_ORIGINS=https://global-olimpiads.biz,https://www.global-olimpiads.biz,https://api.global-olimpiads.biz

# Optional server binding
HOST=0.0.0.0
PORT=3000
